def main():
    """
    Author: Noemi Baruffolo
    date: //2024
    es. 14.1
    text:
    Scrivete una funzione di nome sed che richieda come argomenti una stringa modello, una stringa di sostituzione, e due nomi di file.
    La funzione deve leggere il primo file e scriverne il contenuto nel secondo file (creandolo se necessario). Se la stringa modello
    compare da qualche parte nel testo del file, la funzione deve sostituirla con la seconda stringa. Se si verifica un errore in
    apertura, lettura, scrittura, chiusura del file, il vostro programma deve gestire l'eccezione, stampare un messaggio di errore e
    terminare. Soluzione: http://thinkpython2.com/code/sed.py
    """
    pass #non fa niente, così non da errori nel codice
if __name__ == '__main__':
    main()